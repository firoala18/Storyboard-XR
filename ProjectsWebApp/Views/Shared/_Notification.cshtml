@if (TempData["success"] != null)
{
	<script type="text/javascript">
		(function () {
			var msg = '@Html.Raw(Html.Encode(TempData["success"]))';
			function show() {
				if (window.SB && typeof SB.toast === 'function') { SB.toast(msg, 'success'); }
				else if (typeof toastr !== 'undefined') { toastr.success(msg); }
				else { console.log(msg); }
			}
			if (document.readyState === 'loading') {
				document.addEventListener('DOMContentLoaded', show);
			} else { show(); }
		})();
	</script>
}

@if (TempData["error"] != null)
{
	<script type="text/javascript">
		(function () {
			var msg = '@Html.Raw(Html.Encode(TempData["error"]))';
			function show() {
				if (window.SB && typeof SB.toast === 'function') { SB.toast(msg, 'error'); }
				else if (typeof toastr !== 'undefined') { toastr.error(msg); }
				else { console.log(msg); }
			}
			if (document.readyState === 'loading') {
				document.addEventListener('DOMContentLoaded', show);
			} else { show(); }
		})();
	</script>
}